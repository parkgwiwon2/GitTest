name: released

on:
  release:
    types:
      - released

jobs:
  pre-release:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4
    - run: |
        echo released ${{ github.event.release.tag_name }}
